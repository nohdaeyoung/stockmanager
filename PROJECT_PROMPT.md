# 프로젝트 목표: 미국 주식 데일리 리포트 자동화 시스템 구축

나는 'Prestige Dashboard'라는 주식 포트폴리오 라이브 대시보드 시스템을 구축하고 싶어. 아래 단계에 따라 작업을 한 번에 수행해 줘.

## 1. 환경 설정 및 저장소 연결
- 작업 디렉토리: `/tmp/stockmanager` (또는 워크스페이스)
- GitHub 저장소 연결: `[GitHub 저장소 URL]` (gh-pages 브랜치 사용)

## 2. 파일 구조 초기화
- 대시보드를 위한 `index.html` 파일을 생성해.
- 모든 리포트(Markdown, HTML) 생성 기능은 제외하며, 오직 실시간 데이터와 `latest_kr_prices.json`을 읽어오는 대시보드 UI만 구성해.

## 3. 자동화 스케줄러 (Cron) 등록
다음 조건으로 OpenClaw Cron 작업을 등록해 줘.

- **작업명**: Korea Stock Live Data Update
- **일정**: 매주 월, 화, 수, 목, 금요일 오후 17:00 (KST, Asia/Seoul)
- **대상**: 한국 장이 열리는 날의 마감 데이터 수집
- **실행 로직 (Payload)**:
  1. **데이터 수집**: Naver Finance API 등을 사용해 아래 티커들의 당일 시가(Open), 종가(Close), 변동폭, 고가, 저가를 수집해.
     - **티커 목록**: 088980, 360200, 379800, 395170, 292150, 329200, 364980, 360750, 381180, 148020, 161510, 302450, 381170, 488770, 069500
  2. **데이터 파일 생성**:
     - 대시보드 표시를 위해 수집한 데이터를 `data/latest_kr_prices.json` 포맷으로 덮어써서 저장.
  3. **배포**:
     - 업데이트된 JSON 파일을 `gh-pages` 브랜치에 커밋 & 푸시.
  4. **알림**:
     - 업데이트가 완료되면 텔레그램(`[텔레그램 Chat ID]`)으로 대시보드 링크를 전송해.

[오류 방지 특별 지침]
파이썬이나 JS 스크립트를 작성하여 실행할 때, `exec` 도구에서 `<<'PY'` 방식으로 코드를 직접 넘기지 마시오. 이스케이프 오류가 발생하기 쉽습니다. 반드시 `write` 도구를 사용하여 스크립트를 파일로 명확하게 저장한 후, `exec` 도구로 실행하십시오.

## 4. 즉시 실행 테스트
- 위 Cron 작업이 잘 등록되었는지 확인하고, 테스트 삼아 **오늘 날짜 기준**의 데이터를 수집하여 JSON을 만들고 대시보드를 업데이트한 후 텔레그램 전송까지 완료해 줘.